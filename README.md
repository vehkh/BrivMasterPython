# BrivMasterPython
An AI-generated translation of the BrivMaster AHK script to Python, created to run on Linux. I made little to no manual changes—the code is essentially the raw output of an LLM. I don't claim any credit for the translation itself; this repository simply makes the generated code available.
